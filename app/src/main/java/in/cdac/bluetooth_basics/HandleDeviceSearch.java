package in.cdac.bluetooth_basics;

import android.os.Message;

import java.util.logging.Handler;

public class HandleDeviceSearch extends android.os.Handler {

    @Override
    public void handleMessage(Message msg) {
        switch (msg.what) {
            case 111:

                break;

            default:
                break;
        }
    }
}
